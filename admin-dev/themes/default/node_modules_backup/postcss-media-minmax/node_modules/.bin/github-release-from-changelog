#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../github-release-from-changelog/github-release-from-changelog.js" "$@"
  ret=$?
else 
  node  "$basedir/../github-release-from-changelog/github-release-from-changelog.js" "$@"
  ret=$?
fi
exit $ret
